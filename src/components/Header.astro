---
const navLinks = [
  { name: "Home", href: "/" },
  { name: "Mission", href: "/mission" },
  { name: "Gallery", href: "/gallery-masonry" },
  { name: "Contact", href: "/#contact" },
];
---

<header class="bg-white shadow-md sticky top-0 z-50 animate-fade-in">
  <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
    <a href="/" class="text-2xl font-bold text-gray-900">J&B McKinney Rentals</a>

    <!-- Desktop Menu -->
    <nav class="hidden md:flex space-x-6">
      {navLinks.map(link => (
        <a href={link.href} class="text-gray-700 hover:text-black font-medium transition-all">{link.name}</a>
      ))}
    </nav>

    <!-- Mobile Toggle Button -->
    <button id="menu-toggle" class="md:hidden text-gray-700 hover:text-black focus:outline-none" aria-label="Toggle Menu">
      ☰
      <!-- Alternatively use Heroicon: 
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
      -->
    </button>
  </div>

  <!-- Mobile Menu -->
  <nav id="mobile-menu" class="hidden md:hidden px-4 pb-4 animate-fade-in">
    {navLinks.map(link => (
      <a href={link.href} class="block py-2 text-gray-700 hover:text-black font-medium transition-all">{link.name}</a>
    ))}
  </nav>

  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const toggle = document.getElementById("menu-toggle");
      const menu = document.getElementById("mobile-menu");

      toggle.addEventListener("click", () => {
        menu.classList.toggle("hidden");
      });
    });
  </script>
</header>
